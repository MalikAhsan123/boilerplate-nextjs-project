# API base URL used by RTK Query (default: Next.js mock routes under /api)
NEXT_PUBLIC_API_URL=/api

# Optional: set when connecting to an external backend
# NEXT_PUBLIC_API_URL=https://api.yourcompany.com
